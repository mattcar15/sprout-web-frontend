import React from 'react';

const Loading = () => (
    <div className="container">
        <div className="signin-wrapper">
            <p>Loading...</p>
        </div>
    </div>
);

export default Loading;